import { Stats } from "../api";import { formatRelativeTime } from "../prefs";type Props = {  stats: Stats | null;  status: string;};export default function StatusBar({ stats, status }: Props) {  return (    <footer className="statusbar">      <div className="stats">        {stats ? (          <>            <span>订阅源: {stats.feed_count}</span>            <span>·</span>            <span>文章: {stats.entry_count}</span>            <span>·</span>            <span>未读: {stats.unread_count}</span>            <span>·</span>            <span>收藏: {stats.starred_count}</span>            <span>·</span>            <span>上次同步: {formatRelativeTime(stats.last_synced_at)}</span>          </>        ) : (          <span>加载中…</span>        )}      </div>      {status ? <div className="status-msg">{status}</div> : null}    </footer>  );}
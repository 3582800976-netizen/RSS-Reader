import { useEffect, useState } from "react";import ReactMarkdown from "react-markdown";import remarkGfm from "remark-gfm";import rehypeHighlight from "rehype-highlight";import rehypeSanitize from "rehype-sanitize";import { api, CleanedEntry } from "../api";type Props = {  entryId: number;};export default function MarkdownBody({ entryId }: Props) {  const [cleaned, setCleaned] = useState<CleanedEntry | null>(null);  const [loading, setLoading] = useState(true);  const [retries, setRetries] = useState(0);  useEffect(() => {    let cancelled = false;    let timer: ReturnType<typeof setTimeout> | null = null;    async function fetchCleaned() {      try {        const data = await api.getCleaned(entryId);        if (cancelled) return;        if (data.status === "processing" && retries < 3) {          setRetries((r) => r + 1);          timer = setTimeout(fetchCleaned, 2000);          return;        }        setCleaned(data);      } catch {        if (!cancelled) setCleaned(null);      } finally {        if (!cancelled) setLoading(false);      }    }    fetchCleaned();    return () => {      cancelled = true;      if (timer) clearTimeout(timer);    };  }, [entryId, retries]);  if (loading) {    return <p className="muted">正在清洗文章…</p>;  }  if (!cleaned || !cleaned.cleaned_markdown) {    // Fallback: let parent render original summary    return null;  }  if (cleaned.status === "partial") {    return (      <div className="article-body markdown-body">        <p className="muted cleaning-notice">本文清洗不完整，部分格式可能丢失。</p>        <ReactMarkdown          remarkPlugins={[remarkGfm]}          rehypePlugins={[rehypeHighlight, rehypeSanitize]}        >          {cleaned.cleaned_markdown}        </ReactMarkdown>      </div>    );  }  return (    <div className="article-body markdown-body">      <ReactMarkdown        remarkPlugins={[remarkGfm]}        rehypePlugins={[rehypeHighlight, rehypeSanitize]}      >        {cleaned.cleaned_markdown}      </ReactMarkdown>    </div>  );}
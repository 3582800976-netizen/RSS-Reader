from __future__ import annotationsimport sqlite3from fastapi import APIRouter, Dependsfrom app.db import get_connectionfrom app.schemas import StatsOutrouter = APIRouter(prefix="/api", tags=["stats"])@router.get("/stats", response_model=StatsOut)def get_stats(conn: sqlite3.Connection = Depends(get_connection)) -> StatsOut:    feed_count = conn.execute("SELECT COUNT(*) AS c FROM feeds").fetchone()["c"]    entry_count = conn.execute("SELECT COUNT(*) AS c FROM entries").fetchone()["c"]    unread_count = conn.execute(        "SELECT COUNT(*) AS c FROM entries WHERE is_read = 0"    ).fetchone()["c"]    starred_count = conn.execute(        "SELECT COUNT(*) AS c FROM entries WHERE is_starred = 1"    ).fetchone()["c"]    last = conn.execute(        "SELECT MAX(last_fetched_at) AS t FROM feeds"    ).fetchone()["t"]    return StatsOut(        feed_count=int(feed_count),        entry_count=int(entry_count),        unread_count=int(unread_count),        starred_count=int(starred_count),        last_synced_at=last,    )
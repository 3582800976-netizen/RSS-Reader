from __future__ import annotationsimport sqlite3from fastapi import APIRouter, Depends, HTTPExceptionfrom app.db import get_connectionfrom app.schemas import CleanedOut, ReadingPreferenceIn, ReadingPreferenceOutfrom app.services.cleaning import clean_entryfrom app.services.preferences import get_preferences, ReadingPreferences, save_preferencesrouter = APIRouter(prefix="/api", tags=["cleaning"])@router.get("/entries/{entry_id}/cleaned", response_model=CleanedOut)def get_cleaned(    entry_id: int,    conn: sqlite3.Connection = Depends(get_connection),) -> CleanedOut:    # Verify entry exists    row = conn.execute("SELECT id FROM entries WHERE id = ?", (entry_id,)).fetchone()    if not row:        raise HTTPException(status_code=404, detail="Entry not found")    try:        result = clean_entry(conn, entry_id)    except Exception as exc:        raise HTTPException(status_code=500, detail=f"Cleaning failed: {exc}") from exc    return CleanedOut(        entry_id=entry_id,        cleaned_html=result.cleaned_html,        cleaned_markdown=result.cleaned_markdown,        word_count=result.word_count,        status=result.status,        title=result.title,        byline=result.byline,    )@router.get("/preferences/reading", response_model=ReadingPreferenceOut)def get_reading_preferences(    conn: sqlite3.Connection = Depends(get_connection),) -> ReadingPreferenceOut:    prefs = get_preferences(conn)    return ReadingPreferenceOut(        theme=prefs.theme,        font_size=prefs.font_size,        line_height=prefs.line_height,        font_family=prefs.font_family,        display_mode=prefs.display_mode,        split_ratio=prefs.split_ratio,    )@router.post("/preferences/reading", response_model=ReadingPreferenceOut)def save_reading_preferences(    body: ReadingPreferenceIn,    conn: sqlite3.Connection = Depends(get_connection),) -> ReadingPreferenceOut:    current = get_preferences(conn)    updated = ReadingPreferences(        theme=body.theme if body.theme is not None else current.theme,        font_size=body.font_size if body.font_size is not None else current.font_size,        line_height=body.line_height if body.line_height is not None else current.line_height,        font_family=body.font_family if body.font_family is not None else current.font_family,        display_mode=body.display_mode if body.display_mode is not None else current.display_mode,        split_ratio=body.split_ratio if body.split_ratio is not None else current.split_ratio,    )    saved = save_preferences(conn, updated)    return ReadingPreferenceOut(        theme=saved.theme,        font_size=saved.font_size,        line_height=saved.line_height,        font_family=saved.font_family,        display_mode=saved.display_mode,        split_ratio=saved.split_ratio,    )
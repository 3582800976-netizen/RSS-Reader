from __future__ import annotationsimport asyncioimport loggingimport sqlite3import uuidfrom fastapi import APIRouter, Depends, File, UploadFilefrom fastapi.responses import Responsefrom app.db import connect, get_connectionfrom app.schemas import ImportResultfrom app.services.opml import OPMLFeed, export_opml, parse_opmlfrom app.services.sync import sync_feeds, utc_nowlogger = logging.getLogger(__name__)router = APIRouter(prefix="/api/opml", tags=["opml"])# In-memory tracker for background OPML sync jobs_opml_jobs: dict[str, dict] = {}def _job_result(ok: int, fail: int, total: int, inserted: int = 0) -> dict:    status = "done" if ok + fail >= total else "running"    return {        "status": status,        "total": total,        "synced": ok + fail,        "ok": ok,        "fail": fail,        "inserted": inserted,    }async def _background_sync_opml(job_id: str, feed_ids: list[int], feed_urls: list[str]) -> None:    try:        conn = connect()        feeds = list(zip(feed_ids, feed_urls))        results = await sync_feeds(conn, feeds)        ok = sum(1 for r in results if r[2])        fail = len(results) - ok        inserted = sum(r[3] for r in results)        _opml_jobs[job_id] = _job_result(ok, fail, len(feeds), inserted)    except Exception as exc:        logger.exception("OPML background sync failed for job %s", job_id)        _opml_jobs[job_id] = {            "status": "failed",            "total": len(feed_ids),            "synced": 0,            "ok": 0,            "fail": len(feed_ids),            "inserted": 0,            "error": str(exc),        }    finally:        conn.close()@router.post("/import", response_model=ImportResult)async def import_opml(    file: UploadFile = File(...),    conn: sqlite3.Connection = Depends(get_connection),) -> ImportResult:    raw = await file.read()    feeds = parse_opml(raw)    imported = 0    skipped = 0    feed_ids: list[int] = []    now = utc_now()    for item in feeds:        existing = conn.execute(            "SELECT id FROM feeds WHERE feed_url = ?", (item.feed_url,)        ).fetchone()        if existing:            skipped += 1            feed_ids.append(int(existing["id"]))            continue        cur = conn.execute(            """            INSERT INTO feeds (title, feed_url, site_url, last_fetched_at, created_at)            VALUES (?, ?, ?, NULL, ?)            """,            (item.title, item.feed_url, item.site_url, now),        )        feed_ids.append(int(cur.lastrowid))        imported += 1    conn.commit()    # Kick off background sync for all feeds in the OPML (non-blocking)    all_feed_urls = [item.feed_url for item in feeds]    job_id = uuid.uuid4().hex[:12]    _opml_jobs[job_id] = _job_result(0, 0, len(feed_ids))    asyncio.create_task(_background_sync_opml(job_id, feed_ids, all_feed_urls))    logger.info("OPML import: %d new, %d skipped, job %s started", imported, skipped, job_id)    return ImportResult(imported=imported, skipped=skipped, feed_ids=feed_ids, job_id=job_id)@router.get("/import-status/{job_id}")def import_status(job_id: str) -> dict:    job = _opml_jobs.get(job_id)    if job is None:        return {"status": "unknown"}    return job@router.get("/export")def export_opml_file(conn: sqlite3.Connection = Depends(get_connection)) -> Response:    rows = conn.execute(        "SELECT title, feed_url, site_url FROM feeds ORDER BY id ASC"    ).fetchall()    feeds = [        OPMLFeed(title=r["title"], feed_url=r["feed_url"], site_url=r["site_url"])        for r in rows    ]    xml = export_opml(feeds)    return Response(        content=xml,        media_type="text/xml; charset=utf-8",        headers={"Content-Disposition": 'attachment; filename="subscriptions.opml"'},    )
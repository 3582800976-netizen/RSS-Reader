from __future__ import annotationsimport asyncioimport sqlite3from datetime import datetime, timezonefrom typing import Optionalfrom app.services.feed_parser import ParsedFeed, fetch_and_parsedef utc_now() -> str:    return datetime.now(timezone.utc).isoformat()def insert_entries(conn: sqlite3.Connection, feed_id: int, parsed: ParsedFeed) -> int:    inserted = 0    now = utc_now()    for entry in parsed.entries:        cur = conn.execute(            """            INSERT OR IGNORE INTO entries                (feed_id, guid, url, title, author, published_at, summary, is_read, created_at)            VALUES (?, ?, ?, ?, ?, ?, ?, 0, ?)            """,            (                feed_id,                entry.guid,                entry.url,                entry.title,                entry.author,                entry.published_at,                entry.summary,                now,            ),        )        inserted += cur.rowcount    return insertedasync def sync_feed(conn: sqlite3.Connection, feed_id: int, feed_url: str) -> tuple[bool, int, Optional[str]]:    try:        parsed = await fetch_and_parse(feed_url)        inserted = insert_entries(conn, feed_id, parsed)        conn.execute(            """            UPDATE feeds            SET title = COALESCE(NULLIF(?, ''), title),                site_url = COALESCE(?, site_url),                last_fetched_at = ?            WHERE id = ?            """,            (parsed.title, parsed.site_url, utc_now(), feed_id),        )        conn.commit()        return True, inserted, None    except Exception as exc:  # noqa: BLE001        conn.rollback()        return False, 0, str(exc)async def sync_feeds(    conn: sqlite3.Connection,    feeds: list[tuple[int, str]],    concurrency: int = 6,) -> list[tuple[int, str, bool, int, Optional[str]]]:    sem = asyncio.Semaphore(concurrency)    write_lock = asyncio.Lock()    results: list[tuple[int, str, bool, int, Optional[str]]] = []    async def one(feed_id: int, feed_url: str) -> None:        async with sem:            try:                parsed = await fetch_and_parse(feed_url)            except Exception as exc:  # noqa: BLE001                results.append((feed_id, feed_url, False, 0, str(exc)))                return            async with write_lock:                inserted = insert_entries(conn, feed_id, parsed)                conn.execute(                    """                    UPDATE feeds                    SET title = COALESCE(NULLIF(?, ''), title),                        site_url = COALESCE(?, site_url),                        last_fetched_at = ?                    WHERE id = ?                    """,                    (parsed.title, parsed.site_url, utc_now(), feed_id),                )                conn.commit()            results.append((feed_id, feed_url, True, inserted, None))    await asyncio.gather(*(one(fid, url) for fid, url in feeds))    results.sort(key=lambda r: r[0])    return results
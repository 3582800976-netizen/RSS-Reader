from __future__ import annotationsfrom dataclasses import dataclassfrom datetime import datetime, timezonefrom email.utils import parsedate_to_datetimefrom time import struct_timefrom typing import Any, Optionalimport feedparserimport httpxUSER_AGENT = "MercuryWebReader/0.1 (+local; homework)"@dataclassclass ParsedEntry:    guid: str    url: Optional[str]    title: str    author: Optional[str]    published_at: Optional[str]    summary: Optional[str]@dataclassclass ParsedFeed:    title: str    feed_url: str    site_url: Optional[str]    entries: list[ParsedEntry]def _to_iso(value: Any) -> Optional[str]:    if value is None:        return None    if isinstance(value, datetime):        if value.tzinfo is None:            value = value.replace(tzinfo=timezone.utc)        return value.astimezone(timezone.utc).isoformat()    if isinstance(value, struct_time):        try:            dt = datetime(*value[:6], tzinfo=timezone.utc)            return dt.isoformat()        except Exception:            return None    if isinstance(value, str) and value.strip():        try:            dt = parsedate_to_datetime(value)            if dt.tzinfo is None:                dt = dt.replace(tzinfo=timezone.utc)            return dt.astimezone(timezone.utc).isoformat()        except Exception:            return value.strip()    return Nonedef _entry_summary(entry: Any) -> Optional[str]:    content = getattr(entry, "content", None)    if content and isinstance(content, list) and content:        val = content[0].get("value") if isinstance(content[0], dict) else getattr(content[0], "value", None)        if val:            return str(val)    summary = getattr(entry, "summary", None) or entry.get("summary") if hasattr(entry, "get") else getattr(entry, "summary", None)    if summary:        return str(summary)    description = getattr(entry, "description", None)    if description:        return str(description)    return Nonedef _entry_guid(entry: Any) -> str:    guid = getattr(entry, "id", None) or getattr(entry, "guid", None)    if guid:        return str(guid).strip()    link = getattr(entry, "link", None)    if link:        return str(link).strip()    title = getattr(entry, "title", None) or "untitled"    published = getattr(entry, "published", None) or getattr(entry, "updated", None) or ""    return f"{title}|{published}"def parse_feed_bytes(data: bytes, feed_url: str) -> ParsedFeed:    parsed = feedparser.parse(data)    feed_meta = parsed.feed if getattr(parsed, "feed", None) else {}    title = (        getattr(feed_meta, "title", None)        or (feed_meta.get("title") if hasattr(feed_meta, "get") else None)        or feed_url    )    site_url = (        getattr(feed_meta, "link", None)        or (feed_meta.get("link") if hasattr(feed_meta, "get") else None)    )    entries: list[ParsedEntry] = []    for entry in getattr(parsed, "entries", []) or []:        published = (            getattr(entry, "published_parsed", None)            or getattr(entry, "updated_parsed", None)            or getattr(entry, "published", None)            or getattr(entry, "updated", None)        )        author = getattr(entry, "author", None)        entries.append(            ParsedEntry(                guid=_entry_guid(entry),                url=(getattr(entry, "link", None) or None),                title=(getattr(entry, "title", None) or "(untitled)"),                author=str(author) if author else None,                published_at=_to_iso(published),                summary=_entry_summary(entry),            )        )    return ParsedFeed(        title=str(title).strip() or feed_url,        feed_url=feed_url,        site_url=str(site_url).strip() if site_url else None,        entries=entries,    )async def fetch_and_parse(feed_url: str, timeout: float = 20.0) -> ParsedFeed:    headers = {"User-Agent": USER_AGENT, "Accept": "application/rss+xml, application/atom+xml, application/xml, text/xml, */*"}    async with httpx.AsyncClient(follow_redirects=True, timeout=timeout, headers=headers) as client:        resp = await client.get(feed_url)        resp.raise_for_status()        return parse_feed_bytes(resp.content, str(resp.url) if resp.url else feed_url)